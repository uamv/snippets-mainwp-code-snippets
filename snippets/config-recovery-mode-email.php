/**
 * @title Set Constant: RECOVERY_MODE_EMAIL
 * @description Sets the email address to which recovery mode emails are sent when the site encounters a fatal error
 * @type This Code Snippet goes to the wp-config.php file
 *
 * @replace {{email}} The email address to which recovery mode messages are sent
 */

define( 'RECOVERY_MODE_EMAIL', '{{email}}' ); // Added via MainWP
